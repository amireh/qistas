/** @jsx React.DOM */
define(function(require) {
  var React = require('react');
  var Today = React.createClass({
    render: function() {
      return(
        <div />
      );
    }
  });

  return Today;
});