define(function(require) {
  var Subject = require('jsx!components/backlog');

  describe('Components.Backlog', function() {
    this.reactSuite({
      type: Subject
    });

    it('should render', function() {});
  });
});